<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.my.spring.mappers.boardMapper">

	<!-- 게시물 전체 목록 조회 -->
	<select id="list" resultType="com.my.spring.dto.BoardDTO">
		SELECT bno, title, content, writer, regDate, viewCnt
		FROM tbl_board
		ORDER BY bno DESC
	</select>

	<!-- 게시물 작성 -->
	<insert id="write" parameterType="com.my.spring.dto.BoardDTO">
		INSERT INTO
		tbl_board(title, content, writer)
		VALUES(#{title}, #{content}, #{writer})
	</insert>
	
	<!-- 게시물 상세 조회 -->
	<select id="viewDetail" parameterType="int" resultType="com.my.spring.dto.BoardDTO">
		SELECT title, content, writer, regDate, viewCnt
		FROM tbl_board
		WHERE bno = #{bno}
	</select>
	
	<!-- 조회수 -->
	<update id="views" parameterType="int">
		UPDATE tbl_board
		SET viewCnt = NVL(viewCnt, 0) + 1
		WHERE bno = #{bno}
	</update>
</mapper>